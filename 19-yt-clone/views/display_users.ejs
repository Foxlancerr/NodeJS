<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Users - View - Page</title>
    <style>
      .each {
        background-color: rgb(227, 255, 255);
        margin-bottom: 10px;
        padding: 15px 15px;
        border-radius: 20px;
      }
      .each p {
        color: rgb(43, 43, 43);
      }
    </style>
  </head>
  <body>
    <h1>All Listed Users</h1>
    <div>
      <% users.forEach(user => { %>
      <div class="each">
        <h2><%= user.username%></h2>
        <p><%= user.email%></p>
      </div>
      <% }) %>
    </div>
  </body>
</html>
